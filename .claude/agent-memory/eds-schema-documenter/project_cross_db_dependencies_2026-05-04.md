---
name: Cross-DB dependency graph 2026-05-04
description: Snapshot of strongest cross-database routine couplings on the EDS server, generated by `npm run deps`.
type: project
---

Generated 2026-05-04 by `npm run deps` (src/document-dependencies.js). Output at `docs/dependencies/`.

**Why:** Captures the actual hub-and-spoke shape of the EDS server so future schema/refactor work can predict blast-radius.

**How to apply:** When the user asks about coupling, blast radius of a DB change, or which DBs talk to which, consult these numbers first instead of re-running scans. If the question is current-state and it's been weeks, re-run `npm run deps`.

## Strongest source -> target couplings (distinct edges)

- `EDS` -> `Documents`: 222
- `Catalogs` -> `EDS`: 198
- `EDS` -> `ContentCentral`: 68
- `EDS` -> `VendorBids`: 51
- `VendorBids` -> `EDS`: 34
- `NJ_RTK` -> `EDS`: 31
- `NJ_RTK` -> `Documents`: 19
- `EDS` -> `NJ_RTK`: 16
- `EDS` -> `Catalogs`: 5

## Shape

- `EDS` is the central hub (largest outbound + most inbound).
- `ContentCentral`, `Documents`, `IDIQ_Platform`, `ProcurementAnalytics` have **zero outbound** cross-DB references — they are leaf data stores. (IDIQ_Platform and ProcurementAnalytics are also empty of routines entirely.)
- `Catalogs` -> `EDS` is unexpectedly heavy (198) — likely sync/import procs reaching back into EDS for vendor/customer keys. Worth a closer look if migrating either DB.
- `EDS` -> `Documents` (222) is the hottest single edge.
