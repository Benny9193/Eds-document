# Column Inventory: `*created_`

_Generated on 2026-05-04T15:16:21.246Z_
_Output: `docs\column-inventory-created_.md`_

Pattern `*created_` (SQL LIKE: `%created[_]`) — **0** distinct column names, **0** occurrences across **0** databases.

FK role key: `→ target` = this column is an FK source pointing outward; `← source` = this column is referenced by another table's FK.

_LIKE matching respects the database collation (typically case-insensitive on Azure SQL)._

> No columns matched this pattern.